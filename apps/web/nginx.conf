server {
  listen 8080;
  server_name _;
  root /usr/share/nginx/html;
  index index.html;
  add_header X-Content-Type-Options "nosniff" always;
  add_header X-Frame-Options "DENY" always;
  add_header Referrer-Policy "no-referrer" always;
  add_header Permissions-Policy "camera=(), microphone=(), geolocation=()" always;
  add_header Content-Security-Policy "default-src 'self'; connect-src 'self'; img-src 'self' data:; style-src 'self' 'unsafe-inline'; script-src 'self'" always;

  # Docker 内网 DNS:变量化 proxy_pass 使 api 容器重建后无需重启本容器即可解析新 IP。
  # 纯变量形式原样透传请求 URI(/api/v1/... 前缀与上游一致,无需替换)。
  resolver 127.0.0.11 valid=10s ipv6=off;
  set $api_upstream http://api:8000;
  location /api/v1/ {
    proxy_pass $api_upstream;
    proxy_set_header Host $host;
  }
  location / { try_files $uri $uri/ /index.html; }
}

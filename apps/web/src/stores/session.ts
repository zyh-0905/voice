import {defineStore} from 'pinia';import {ref} from 'vue';export const useSessionStore=defineStore('session',()=>{const user=ref<{id:string;name:string;email:string}|null>(null);const isDemo=ref(true);function login(){user.value={id:'demo-user',name:'Demo Reviewer',email:'demo@voicelens.local'}}function logout(){user.value=null}return {user,isDemo,login,logout};});

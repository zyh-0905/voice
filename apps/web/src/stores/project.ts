import {defineStore} from 'pinia';import {ref,computed} from 'vue';export const useProjectStore=defineStore('project',()=>{const projects=ref([{id:'demo-project',name:'VoiceLens Demo Project',description:'Synthetic workspace'}]);const selectedProjectId=ref('demo-project');const selectedProject=computed(()=>projects.value.find(p=>p.id===selectedProjectId.value)??projects.value[0]);return {projects,selectedProjectId,selectedProject};});
